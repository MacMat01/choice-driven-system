# Legacy API (_Old)

Everything in this folder is deprecated and kept only for migration support.

- Runtime types in `_Old/Runtime` are annotated with `[Obsolete]`.
- New development should target the refactored APIs in `Runtime/` and `Editor/`.
- This folder may be removed in a future major release.

